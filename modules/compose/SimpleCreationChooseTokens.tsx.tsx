import { Box, Heading, Text, VStack } from '@chakra-ui/react';
import React from 'react';
import Card from '~/components/card/Card';

interface Props {}

export default function SimpleCreationChooseTokens(props: Props) {
    return (
        <VStack width="full">
            <Card width="50%">
                <Heading>fds </Heading>
            </Card>
        </VStack>
    );
}
